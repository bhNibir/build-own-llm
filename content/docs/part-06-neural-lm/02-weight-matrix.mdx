---
title: Weight Matrix W
description: Embedding vector থেকে next-token logits — neural bigram
---

# ⚖️ Weight Matrix W

<ConceptAnim name="next-token" caption="Weight matrix → logits" />

Embedding vector পেলাম — এখন **next token predict** করতে হবে। Weight matrix `W` embedding-কে **logits**-এ convert করে।

<Callout type="warn">
⚠️ এটা essentially **neural bigram**: current token-এর embedding দেখে পরের token guess। Context window এখনো 1 — Attention পরে আসবে।
</Callout>

```mermaid
flowchart LR
    A["📊 embed(x)"]:::input --> B["⚖️ W @ embed"]:::matmul
    B --> C["📈 logits"]:::output
    C --> D["🎯 softmax → probs"]:::softmax
    classDef input fill:#4F46E5,color:#fff,stroke:#3730A3
    classDef matmul fill:#10B981,color:#fff,stroke:#047857
    classDef output fill:#6366F1,color:#fff,stroke:#4338CA
    classDef softmax fill:#F59E0B,color:#fff,stroke:#D97706
```

## Shape

$$W \in \mathbb{R}^{D \times V}$$

- Input: embedding vector shape `(D,)`
- Output: logits shape `(V,)` — প্রতিটা vocabulary word-এর জন্য একটা score

## Example Walkthrough

Playground-এ fruit words + D = 3 embedding:

```
embed("i")    = [1, 0, 0]  → argmax next ≈ "like"
embed("like") = [0, 1, 0]  → argmax next ≈ "apple"
embed("fruit")= [0, 0, 1]  → argmax next ≈ "is"
```

General form: `logits = W @ embed(x)`, then `softmax(logits)` → next-word probabilities.

<Illustration name="matrix-grid" />

## Softmax = Probability

Raw logits negative/positive যেকোনো হতে পারে। **Softmax** সব positive probability-তে convert করে (sum = 1):

$$\text{softmax}(z_i) = \frac{e^{z_i}}{\sum_j e^{z_j}}$$

## Interactive Demo

<Playground id="part-06/weight-matrix" title="Weight Matrix Forward Pass" />

## ✅ তুমি কী শিখলে?

- **W** maps embedding → logits over vocabulary
- Forward pass = embed → matmul → softmax
- Neural bigram = learned weights, not counts

**পরের lesson:** [Cross-Entropy Loss](/docs/part-06-neural-lm/03-loss)
